# P0 严重问题修复实施计划

> **创建日期**: 2025-11-13  
> **预计完成时间**: 46-62 工作日  
> **目标**: 确保数据安全和系统稳定性

---

## 📋 执行摘要

### 当前状态分析

经过代码审查，发现以下情况：

1. **崩溃恢复框架已存在** ✅
   - `crash_recovery.go` 已实现 ARIES 三阶段恢复
   - `redo_log_manager.go` 已有 `RecoverWithCrashRecovery` 方法
   - 但缺少完整的测试和验证

2. **MVCC ReadView 已修复** ✅
   - `mvcc_manager.go` 第 68-111 行已修复 ReadView 创建逻辑
   - 正确记录活跃事务列表

3. **Gap 锁框架已存在** ⚠️
   - `lock_manager.go` 已定义 Gap 锁数据结构
   - 但实现不完整，需要补充核心逻辑

4. **二级索引维护缺失** ❌
   - 完全未实现
   - 需要从零开始实现

---

## 🎯 任务1：完善日志恢复机制 (12-16天)

### 1.1 Redo 日志重放完善 (4-5天)

**当前状态**: 基础框架已存在，需要完善和测试

**待完成工作**:

1. **完善 Redo 日志类型支持** (1天)
   - 文件: `server/innodb/manager/redo_log_manager.go`
   - 确保支持所有 15 种日志类型
   - 添加日志类型验证

2. **实现幂等性保证** (1天)
   - 文件: `server/innodb/manager/crash_recovery.go`
   - 完善 `redoInsert/redoUpdate/redoDelete` 方法
   - 确保 LSN 检查正确

3. **优化 Redo 性能** (1天)
   - 实现批量 Redo
   - 添加并行 Redo 支持（可选）

4. **添加 Redo 日志验证** (1-2天)
   - 校验和验证
   - 日志完整性检查

### 1.2 Undo 日志回滚完善 (4-5天)

**当前状态**: 框架存在，但回滚逻辑不完整

**待完成工作**:

1. **实现完整的 Undo 记录应用** (2天)
   - 文件: `server/innodb/manager/undo_log_manager.go`
   - 完善 `Rollback` 方法
   - 实现版本链遍历

2. **实现 CLR (Compensation Log Record)** (1天)
   - 确保回滚操作可恢复
   - 写入 CLR 到 Redo 日志

3. **优化回滚性能** (1-2天)
   - 批量回滚
   - 回滚进度跟踪

### 1.3 崩溃恢复测试 (4-6天)

**待完成工作**:

1. **单元测试** (2天)
   - 测试 Analysis 阶段
   - 测试 Redo 阶段
   - 测试 Undo 阶段

2. **集成测试** (2天)
   - 完整崩溃恢复流程测试
   - 多事务并发崩溃测试

3. **故障注入测试** (2天)
   - 模拟各种崩溃场景
   - 验证数据一致性

---

## 🎯 任务2：修复 MVCC 和锁管理 (7-9天)

### 2.1 ReadView 创建逻辑 (已完成) ✅

**状态**: 已在 `mvcc_manager.go` 中修复

### 2.2 实现 Gap 锁 (4-5天)

**当前状态**: 数据结构已定义，核心逻辑缺失

**待完成工作**:

1. **实现 Gap 锁范围确定** (2天)
   - 文件: `server/innodb/manager/lock_manager.go`
   - 实现 `AcquireGapLock` 方法
   - 实现 Gap 范围计算

2. **实现 Gap 锁冲突检测** (2天)
   - 实现 `CheckGapLockConflict` 方法
   - 集成到锁管理器

3. **测试 Gap 锁** (1天)
   - 幻读防止测试
   - 并发 Gap 锁测试

### 2.3 实现 Next-Key 锁 (3-4天)

**待完成工作**:

1. **实现 Next-Key 锁逻辑** (2天)
   - 结合 Record 锁和 Gap 锁
   - 实现 `AcquireNextKeyLock` 方法

2. **测试 Next-Key 锁** (1-2天)
   - REPEATABLE READ 隔离级别测试
   - 幻读防止验证

---

## 🎯 任务3：修复存储和索引问题 (7-9天)

### 3.1 实现二级索引维护 (5-6天)

**当前状态**: 完全未实现

**待完成工作**:

1. **设计二级索引维护接口** (1天)
   - 定义 `SecondaryIndexMaintainer` 接口
   - 设计数据结构

2. **实现 INSERT 时的二级索引维护** (2天)
   - 文件: `server/innodb/manager/index_manager.go`
   - 同步插入到所有二级索引

3. **实现 UPDATE 时的二级索引维护** (2天)
   - 删除旧索引项
   - 插入新索引项

4. **实现 DELETE 时的二级索引维护** (1天)
   - 从所有二级索引删除

### 3.2 修复表空间扩展并发问题 (1-2天)

**待完成工作**:

1. **添加扩展锁** (1天)
   - 文件: `server/innodb/manager/space_expansion_manager.go`
   - 使用互斥锁保护扩展操作

2. **测试并发扩展** (1天)
   - 并发扩展测试
   - 数据一致性验证

### 3.3 实现脏页刷新策略 (1-2天)

**待完成工作**:

1. **实现自适应刷新** (1天)
   - 文件: `server/innodb/buffer_pool/buffer_lru.go`
   - 根据脏页比例动态调整刷新速率

2. **测试刷新策略** (1天)
   - 性能测试
   - 抖动测试

---

## 📅 时间线

| 周次 | 任务 | 预计完成 |
|------|------|---------|
| 第1周 | 任务1.1 Redo日志完善 | 4-5天 |
| 第2周 | 任务1.2 Undo日志完善 | 4-5天 |
| 第3周 | 任务1.3 崩溃恢复测试 | 4-6天 |
| 第4周 | 任务2.2 Gap锁实现 | 4-5天 |
| 第5周 | 任务2.3 Next-Key锁实现 | 3-4天 |
| 第6周 | 任务3.1 二级索引维护 (上) | 3天 |
| 第7周 | 任务3.1 二级索引维护 (下) | 2-3天 |
| 第8周 | 任务3.2-3.3 存储优化 | 2-4天 |

**总计**: 46-62 工作日 (约 2-3 个月)

---

## ✅ 验收标准

### 任务1: 日志恢复
- [ ] 所有 15 种日志类型都能正确重放
- [ ] 崩溃恢复测试 100% 通过
- [ ] 故障注入测试覆盖 10+ 种场景
- [ ] 恢复时间 < 1分钟 (对于 1GB 日志)

### 任务2: MVCC 和锁
- [ ] Gap 锁能防止幻读
- [ ] Next-Key 锁在 RR 隔离级别下正常工作
- [ ] 死锁检测正常工作
- [ ] 并发锁测试通过

### 任务3: 存储和索引
- [ ] 二级索引与主索引保持一致
- [ ] DML 操作后索引数据正确
- [ ] 并发扩展不会导致数据覆盖
- [ ] 脏页刷新不会导致性能抖动

---

## 🚀 下一步行动

1. **立即开始**: 任务1.1 Redo 日志完善
2. **准备环境**: 搭建测试环境
3. **代码审查**: 详细审查现有代码
4. **编写测试**: 先写测试，再修复代码 (TDD)


